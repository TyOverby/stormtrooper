(set default-unit px)

(define quickline (lambda (x)
    (cut-line
        x
        0
        0
        150)))

(quickline 0)
(quickline 12)
(quickline 50)
(quickline 201)

(cut-line
    100
    0
    0
    150)


(cut-line
    (px 0)
    (px 50)
    (px 150)
    (px 50))
